name: movies_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # 🧊 Freezed for immutable models
  freezed_annotation: ^2.4.1

  # 🌱 Riverpod for state management
  flutter_riverpod: ^2.4.0

  # 🌐 HTTP for API calls
  http: ^1.2.1

  # 🧪 JSON serialization (updated to avoid warnings)
  json_annotation: '>=4.9.0 <5.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # 🧊 Freezed code generator
  build_runner: ^2.4.6
  freezed: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

  